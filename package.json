{
  "name": "cap_gestion_ticket",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/postgres": "^1.3.1",
    "@sap/cds": "^7",
    "@sap/textbundle": "^4.3.0",
    "@sap/xsenv": "^4.1.0",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7",
    "cds-swagger-ui-express": "^0.6.0"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "postgres",
        "credentials": {
          "host": "localhost",
          "port": 5432,
          "database": "gestionTicketDB",
          "user": "postgres",
          "password": "admin"
        }
      }
    }
  }
}
